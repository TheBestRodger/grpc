ПО ожидает по tcp данные(пиксели) от зарницы, обрабатывает их, формирует структуру OESKN_out и отправляет ее в ММ65

В niui.cpp указать свой путь до onnx модели

В settings.cmake прописать статичные пути сборки библиотек.